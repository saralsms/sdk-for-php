os: linux
language: php
php:
  - 5.5
  - 5.6
  - 7.0
  - 7.1
  - 7.2
  - 7.3
  - 7.4

before_script:
  - composer self-update
  - composer install --dev --optimize-autoloader --no-interaction

script:
  - ./vendor/bin/phpunit

notifications:
  email:
    on_success: always
    on_failure: always
